# app/services/payment_processor.rb
class PaymentProcessor
  def process_payment(customer, amount)
    # Logic to process payment (e.g., call to payment gateway API)
    true  # For simplicity, we assume payment is always successful
  end
end
